# Basic GCE Example

This example will create a fully functional GCE Instance.

## Usage

Create Instance:

```
$ terraform init
$ terraform plan
$ terraform apply
```

Show Information:

```
$ terraform show
```

Destroy Instance:

```
$ terraform destroy
```
